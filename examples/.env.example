# Team Simulation Project - Environment Variables Example
# Copy this file to .env and fill in your API keys

# OpenAI API Key (Required for CrewAI)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify the OpenAI model to use
# If not specified, will use the default model (usually gpt-3.5-turbo)
OPENAI_MODEL=gpt-4

# Optional: Maximum number of tokens per API call
# This can help control costs for larger simulations
MAX_TOKENS=4000

# Optional: Rate limiting to prevent hitting OpenAI rate limits
# Number of seconds to wait between API calls
API_CALL_DELAY=0.5 